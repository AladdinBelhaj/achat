pipeline {
    agent any

    stages {
      stage('Code checkout from GitHub') {
        steps {
            git branch: 'main', url:  'https://github.com/AladdinBelhaj/achat.git'
          }
      }
   }
}
